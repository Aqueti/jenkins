#!groovy

//node {
//   echo "Hello from the Aqueti build pipeline! This is a linux machine"
//}

try {
   node {
      stage 'Stage1'
      echo "Build Stage"
   }
   catch (exc) {
      err = caughtError
   }
}
